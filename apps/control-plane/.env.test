# Test Environment Configuration
PORT=4001
NODE_ENV=test
DATABASE_URL=postgresql://postgres:temporary_battery_horse_staple_password@localhost:5432/escapehatch_test
SFU_TOKEN_TTL_SECONDS=60
DEV_AUTH_BYPASS=true
SETUP_BOOTSTRAP_ENABLED=true
SETUP_BOOTSTRAP_TOKEN=test_bootstrap_token
# Include other required vars from root .env if needed, but DATABASE_URL is the critical one for isolation.
