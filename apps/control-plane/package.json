{
  "name": "@escapehatch/control-plane",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "dotenv -e ../../.env -- tsx watch src/index.ts",
    "build": "tsc --project tsconfig.json",
    "start": "dotenv -e ../../.env -- node dist/index.js",
    "lint": "tsc --project tsconfig.json --noEmit",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "dotenv -e ../../.env -- tsx --test src/test/*.test.ts"
  },
  "dependencies": {
    "@escapehatch/shared": "workspace:*",
    "@fastify/cors": "^10.0.2",
    "@fastify/sensible": "^6.0.2",
    "fastify": "^5.1.0",
    "zod": "^3.24.1",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "dotenv-cli": "^7.4.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "@types/pg": "^8.11.10"
  }
}
